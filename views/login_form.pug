extends layout 

block content
  div.container.form-centered.mt-3.mb-3
    form(method="post")
      h1.main-heading Log In
      div.form-group
        label(for="username") Username
        input(class="form-control" name="username")

      div.form-group
        label(for="username") Username
        input(class="form-control" name="password")

      button.btn.btn-primary Log In